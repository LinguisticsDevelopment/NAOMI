# Spanish Encoder Gold v2 — Candidate Lattice Stats

Generated by `scripts/build_encoder_gold_es.py` from the same canonical `dev/ENCODER_IO_CONTRACT_V2.md` v2 record shape as the English builder (`scripts/build_encoder_gold_v2.py`), for the grammar-swap test: train the encoder on English, test on Spanish with the SAME weights. Source: `nsm_ct.curriculum2.TEMPLATES_ES`/`TRANSFER_TEMPLATES_ES` (correctly accented). Numbers only.

- USVS fingerprint at build time: `e0daef638b640dd5` (see Task 1 verification below for before/after proof it is unchanged by the additive Spanish lemma-index extension)
- Unique deduped Spanish template sentences generated: 208
- Records emitted (valid lattice, >=1 tree): 208
- Outcome counts: ok=208

## Task 1 — USVS Spanish lemma resolution (additive, fingerprint-safe)

Implementation: `src/nsm_ct/wordnet.py` gained `spanish_lemmas(sense_id)` (OMW `lemmas(lang="spa")` per synset, graceful-empty like every other helper in that module). `src/nsm_ct/ground/usvs.py`'s `build_usvs` sense loop unions those Spanish lemmas into `sense_lemmas` alongside the existing English lemmas, keyed to the SAME `sense_id` -- no re-grounding. The sense-grounding weights (`sense_usvs_weights`) never read the `lemmas` list (the synset's own lemmas are deliberately excluded from grounding, per that function's own docstring), and the fingerprint hashes only `axes` names + `meta` counts -- neither depends on lemma content -- so this change is PROVABLY fingerprint-inert, not just measured-inert.

Verified by full before/after USVS rebuild (`python scripts/build_usvs.py`, same 9,946 core words / 607 axes / 117,659 senses both times):

| | before | after |
|---|---|---|
| fingerprint | `e0daef638b640dd5` | `e0daef638b640dd5` (IDENTICAL) |
| `senses_of('dog')` | `dog.n.01, frump.n.01, dog.n.03, cad.n.01, frank.n.02` | same |
| `senses_of('cat')` | `cat.n.01, guy.n.01, cat.n.03, kat.n.01, cat-o'-nine-tails.n.01` | same |
| `senses_of('run')` | `run.n.01, test.n.05, footrace.n.01, streak.n.01, run.n.05` | same |
| `senses_of('happy')` | `happy.a.01, felicitous.s.01, glad.s.01, happy.s.03` | same |
| `senses_of('bank')` | `bank.n.01, depository_financial_institution.n.01, bank.n.03, bank.n.04, bank.n.05` | same |
| `senses_of('perro')` | `[]` | `dog.n.01, rotter.n.01` |
| `senses_of('gato')` | `[]` | `cat.n.01, caterpillar.n.02, dodger.n.01, kitty.n.04, tom.n.02` |
| `senses_of('casa')` | `[]` | `building.n.01, diggings.n.02, dwelling.n.01, family.n.01, firm.n.01` |
| `senses_of('niño')` | `[]` | `baby.n.01, chap.n.01, child.n.01, child.n.02, child.n.03` |

English senses_of unchanged (5/5 exact match); Spanish now resolves (4/4 test words, matching `dev/SPANISH_SWAP_FEASIBILITY.md`'s standalone-probe numbers exactly, now folded into the production USVS build instead of a throwaway script).

## Grounding-type breakdown (all nodes: predicate + roles, across all trees)

- `entity`: 892 (71.0%)
- `sense`: 365 (29.0%)

## Predicate-verb grounding: sense vs. entity fallback

Conjugated Spanish predicate verbs (está/fue/tomó) are NOT base OMW-es dictionary lemmas, so they don't hit the lemma index and correctly fall back to `type:"entity"` per contract §4.2's documented ungrounded-content-word case -- this is the one narrower, separately-scoped gap `dev/SPANISH_SWAP_FEASIBILITY.md` flags (verb-conjugation generation is out of scope for this task, and for `scripts/build_parser_lexicon.py`'s own stated scope).

- predicate grounding `entity`: 346 (100.0%)

## Forest width (trees per sentence, after clause-level dedup)

- median: 1.0
- p90: 2.0
- max: 4
- 1-tree sentences (no surviving structural ambiguity): 108/208

## Raw parser hypotheses per sentence (before clause extraction/dedup)

- median: 2.0
- p90: 4.0
- max: 4

## Sense-candidate breadth (per node grounded `type:"sense"`)

- n sense-grounded nodes: 365
- median candidates/node: 5.0
- p90 candidates/node: 13.0

## Contract-shape validation

Every emitted record was validated by round-tripping it through the real reader, `nsm_ct.encoder_model._gold_sites`: **208/208 pass, 0 exceptions.**

Of the 208 emitted records, 90 had only 1 raw parser hypothesis (genuinely unambiguous parse, not a top-k extraction limitation -- same reachability note as the English builder: `ParserInputEncoder._parse_topk_one` already exposes `chart.hypotheses[:k]` for both languages, no parser-internal change needed).
